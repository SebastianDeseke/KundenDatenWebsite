@page
@model PrivacyModel
@{
    ViewData["Title"] = "Privacy Policy";
}
<h1>@ViewData["Title"]</h1>

<p>Use this page to detail your site's privacy policy.</p>

</br></br>
<h3>English</h3>
<p>In the sites current state, information is only saved to the database when a user registers. This information is only used to identify the user and is 
    <strong>not shared with any third parties</strong>.</p>
</br>
<p>The Data is saved and used by the Localhost, and thus poses no thret to you online or mental welbeing. Most Data is used as "Mockdata", and is thus mostly made up or just random.</p>

</br></br>

<h3>Deutsch</h3>
<p>In dem aktuellen Zustand der Website werden Informationen nur in der Datenbank gespeichert, wenn sich ein Benutzer registriert. Diese Informationen werden nur zur Identifizierung 
    des Benutzers verwendet und werden <strong>nicht an Dritte Parteien weitergegeben</strong>.</p>
</br>
<p>Die Daten werden von Localhost gespeichert und verwendet und stellen daher keine Bedrohung für Ihre Online- oder geistige Gesundheit dar. Die meisten Daten werden als "Mockdaten" 
    verwendet und sind daher größtenteils erfunden oder zufällig generiert worden.</p>

    <!-- Button trigger modal -->
<button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#staticBackdrop">
  Launch static backdrop modal
</button>

<!-- Modal -->
<div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="staticBackdropLabel">Modal title</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        ...
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Understood</button>
      </div>
    </div>
  </div>
</div>